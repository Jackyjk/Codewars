function evaluate (arr) {
    //TODO
    // let numStack = [], strStack = []
    // for (let i = 0; i < arr.length; i++) {
    //     if (!isNaN(arr[i])) numStack.push(arr[i])
    //     else strStack.push(arr[i])
    // }
    // let flag = true
    // while (flag){

    // }
    let num = arr.join("")
    return num
}

console.log(evaluate(['10', '+', '20', '*', '3']))
console.log(evaluate(['10', '+', '20', '*', '3', '*']))